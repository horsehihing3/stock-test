# 클로드 2호(Claude_2) 설계안 + 실측 결과

> 선행 문서: `BRIEFING_Claude_2.md`
> 상태: **구현 완료.** 설계(§1~7)는 원안 그대로 두고, 실측 결과를 §9~12 에 추가했다
> 평가 구간: **5.7Y** (2021-01-01 ~ 2026-09-20), 손절 5% 기준

## 요약 — 합격 기준 4개 중 3개 통과

| # | 기준 | 결과 | 판정 |
|---|---|---|---|
| 1 | BTC Calmar ≥ 0.71 | **0.90** | ✅ |
| 2 | 나머지 3종목 Calmar 격차 축소 | NVDA +0.15 · AAPL +0.17 **역전** / QQQ -0.17 | ❌ QQQ 미달 |
| 3 | 4종목 회복 1000일 이내 · 미회복 0 | 최장 **602일**, 미회복 **0/4** | ✅ |
| 4 | 4종목 MDD -30% 이내 | -23.45 / -22.68 / -10.32 / -13.75 | ✅ |

미달·한계 5개는 §12 에 그대로 남겼다.

---

**전략명**: 변동성 예산 기반 조기 재진입 (Volatility-Budgeted Early Re-entry)

한 줄 요약: **이동평균 위치를 진입 조건에서 완전히 제거하고, 변동성 수축과
국소 저점 대비 ATR 단위 반등만으로 재진입한다. 낙폭이 깊을수록 요구 반등폭이
자동으로 커지며, 위험은 단계적 축소로 관리한다.**

---

## 0. ⚠️ 먼저 보고 — 브리핑 §9의 "부분 진입 허용"은 현 엔진에서 성립하지 않는다

`backtest.py` 116~129행을 확인한 결과:

```python
for row in rows:
    if shares > 0:
        sig = strat.exit(row, state)      # 보유 중에는 exit() 만 호출
        ...
    elif strat.entry(row):                # elif — 보유 중이면 entry() 자체가 호출 안 됨
        shares = cash / (px * (1 + FEE))  # 진입은 항상 현금 100%
```

- `entry()` 는 **bool 만 반환**하고 엔진은 **무조건 현금 전액**을 투입한다
- 보유 중에는 `entry()` 가 호출되지 않는다 → **피라미딩·추가 매수 불가**
- 분할 청산으로 생긴 현금은 **전량 청산 전까지 재투입되지 않는다**

즉 포지션 크기는 **진입 시 100%에서 시작해 내려가기만 하는 래칫**이다.
브리핑 §9의 "부분 진입 허용 · 제미나이 4호에서 검증됨"은 부분 **청산**을 부분
**진입**으로 오기한 것이다.

**이 설계안은 엔진 수정 없이 성립하도록 래칫 구조만 사용한다.** (§4-4)
확장이 있으면 더 좋아질 여지는 §7에 따로 적었다.

---

## 1. 어느 빈틈을 노리는가

**주 타깃: §10 회복기간 문제 (재진입 시점).**
보조: **§7-C** (변동성 적응) + **§7-D** (분할 — 단, 청산 측만).
제외: **§7-A** (숏 금지), **§7-E** (해당 없음).

### 왜 회복기간인가

최장회복일은 **자산 가격이 아니라 전략 자산곡선(equity)** 위에서 측정된다.
현금 보유 구간에서 equity 는 평평하다 — **평평한 선은 고점을 회복하지 못한다.**
기존 전략이 9/20 조합에서 미회복인 이유가 여기 있다. 하락을 피한 것까지는
성공했지만, 회복 구간에 들어가 있지 않으면 드로다운은 영원히 닫히지 않는다.

**회복기간을 줄이는 유일한 방법은 회복 구간에 투자되어 있는 것이다.**
이것이 이 설계의 출발점이다.

### 왜 기존 5개와 상관이 낮은가

기존 5개는 **전부 장기 이동평균 위치를 진입 게이트로 쓴다**:

| 전략 | 게이트 |
|---|---|
| 제미나이 1호 | 종가 > SMA50 |
| 제미나이 2호 | SMA20 상승 중 |
| 제미나이 3호 | 종가 > SMA50 & SMA20 상승 |
| 제미나이 4호 | 종가 > EMA200 |
| 클로드 1호 | 종가 > SMA100 & EMA8/21 골든크로스 |

**클로드 2호는 이동평균을 진입 조건에서 완전히 뺀다.** 대신 세 가지만 본다:

1. 변동성 백분위 (ATR% 의 252봉 트레일링 순위)
2. 국소 저점 대비 상승폭 (ATR 단위)
3. 252봉 고점 대비 낙폭 깊이 — 이것이 1·2의 문턱을 조정한다

이동평균 게이트는 구조적으로 **바닥 이후 수십 일 뒤에야 열린다.**
2호의 게이트는 **바닥 직후 열릴 수 있다.** 2022년 하락장에서 기존 5개는 전부
게이트가 닫혀 83~99% 현금이었으므로, **보유 구간 자체가 겹치지 않는다.**
이것이 낮은 상관의 구조적 근거다 (사후 상관계수는 2단계에서 측정).

---

## 2. 재진입 조건 — 이번 과제의 핵심

### 2-1. 얼마나 앞당겨지는가 (실측)

2021-11 고점 → 2022 바닥 이후, 클로드 1호 방식(SMA100 국면 + EMA8/21 골든크로스)과
2호 제안 방식의 첫 재진입 시점을 실제 데이터로 비교했다:

| 종목 | 바닥 | 클로드 1호식 재진입 | 2호안 재진입 | 먼저 탑승 |
|---|---|---|---|---|
| BTC-USD | 2022-11-21 (-76.6%) | +86일, 바닥 대비 **+54.0%** | +22일, **+12.6%** | **41.4%p** |
| NVDA | 2022-10-14 (-66.3%) | +90일, **+47.1%** | +14일, **+23.2%** | 23.9%p |
| AAPL | 2023-01-05 (-30.3%) | +57일, **+21.0%** | +8일, **+7.8%** | 13.2%p |
| QQQ | 2022-11-03 (-35.1%) | +123일, **+15.5%** | +14일, **+9.3%** | 6.2%p |

(2호안 = 낙폭 배율 kdd=6 적용 기준. §2-3 참조)

기존 방식은 바닥에서 **15~54% 오른 뒤에야** 올라탄다. 2호안은 **8~23%**.
QQQ 는 123일 → 14일로 109일을 단축한다. 이 차이가 회복기간 격차의 실체다.

### 2-2. 무엇으로 앞당기는가

이동평균이 방향을 바꾸기를 기다리지 않는다. 대신 **패닉이 끝났다는 신호**를 본다:

```
lift    = 종가 - 최근20봉 최저종가        # 국소 저점에서 얼마나 들렸나
need    = base_k × (1 + kdd × dd) × ATR   # 요구 반등폭 (ATR 단위, 낙폭에 비례)
vol_ok  = ATR%의 252봉 백분위 < 0.5       # 변동성이 평소 수준 이하로 수축

재진입 = (lift >= need) AND vol_ok
```

이동평균·MACD·RSI 를 쓰지 않는다. **가격이 방향을 증명하기 전에, 공포가
가라앉았다는 것만 확인하고 들어간다.**

### 2-3. 앞당기면 가짜 반등에 물린다 — 그 대가를 무엇으로 치르는가

**문제는 실측으로 확인됐다.** 문턱을 고정하면(kdd=0) 2021-11~2022-11 하락
구간에서 오발이 폭증한다:

| 종목 | 하락 구간 오발 | 신호 후 20봉 음수 비율 | 평균 | 최악 |
|---|---|---|---|---|
| BTC-USD | **31회** | 77% | -9.3% | -35.2% |
| NVDA | 5회 | 40% | -0.2% | -30.1% |
| AAPL | 3회 | 33% | -0.1% | -14.1% |
| QQQ | 4회 | **100%** | -4.2% | -7.7% |

BTC 31회 오발은 그대로면 치명적이다. **대가는 세 가지로 나눠 치른다.**

**(a) 낙폭 비례 문턱 — 주 방어선**

`need = base_k × (1 + kdd × dd) × ATR`. 낙폭 `dd` 가 깊을수록 요구 반등폭이 커진다.
얕은 조정에서는 즉시 복귀하고, 깊은 약세장에서는 더 확실한 증거를 요구한다.
kdd 별 실측:

| kdd | BTC 오발 | BTC 재진입 | NVDA 오발 | NVDA 재진입 | AAPL·QQQ |
|---|---|---|---|---|---|
| 0 | 31회 | 바닥+2일 (+5.2%) | 4회 | +7일 (+11.0%) | 변화 없음 |
| 3 | 18회 | +9일 (+8.7%) | 1회 | +11일 (+18.1%) | 거의 없음 |
| **6** | **7회** | **+22일 (+12.6%)** | 4회 | +14일 (+23.2%) | 없음 |
| 10 | 4회 | +51일 (+13.6%) | 1회 | +27일 (+40.3%) | 미미 |

**kdd=6 을 기본값으로 제안한다.** BTC 오발을 31→7회로 줄이면서도 재진입은
바닥+22일(클로드 1호 대비 64일 빠름)을 유지한다.

주목할 점: **AAPL·QQQ 는 kdd 를 올려도 거의 변하지 않는다.** 두 종목의 최대 낙폭이
30~35% 로 얕아 `(1 + 6×dd)` 항이 작게 작동하기 때문이다. **티커 분기문 없이
자산 특성만으로 종목별 반응이 달라지는 것** — 브리핑 §8-2 가 요구한 형태다.

(kdd 최종값은 2단계에서 3/6/10 스윕 후 §6-5 원칙대로 *일관성* 기준으로 확정한다.
여기 적은 6 은 설계 기준값이지 튜닝 결과가 아니다.)

**(b) 수습 기간 타이트 손절 — 보조 방어선**

진입 직후 포지션이 스스로를 증명하기 전까지는 손절을 좁게 잡는다.
오발 1회당 손실을 ATR 1.2배로 제한하면, BTC 기준 회당 약 -4~5% 다.
7회 오발이면 누적 -30% 수준 — 감당 가능한 범위로 내려온다.
(고정 문턱 kdd=0 이었다면 31회 × 평균 -9.3% 로 복구 불가능했다.)

**(c) 위치 크기 축소는 쓸 수 없다**

부분 진입이 가능했다면 "확신이 약할 때 30%만 진입"이 가장 깔끔한 대가 지불
방식이었다. **§0 에서 확인했듯 현 엔진은 이를 지원하지 않는다.**
따라서 (a)+(b) 로 대체한다 — 크기 대신 **빈도와 손실폭**으로 지불한다.

---

## 3. 종목 특성 자동 조정

### 3-1. 무엇을 측정하는가

| 측정값 | 정의 | 용도 |
|---|---|---|
| `atr_pct` | `ATR(14) / 종가` | 변동성의 크기 (절대 가격 무관) |
| `vol_rank` | `atr_pct` 의 **252봉 트레일링 백분위** (0~1) | 그 자산 기준 "지금 변동성이 평소보다 높은가" |
| `dd` | `1 - 종가 / 252봉 최고종가` (0~1) | 그 자산이 지금 얼마나 깊이 빠져 있나 |

셋 다 **자기 자신의 과거와 비교하는 상대값**이다. BTC 의 ATR% 는 QQQ 의 3배지만
`vol_rank` 는 둘 다 0~1 이다. **같은 코드가 데이터만 보고 다르게 반응한다.**

### 3-2. 무엇을 조정하는가

```
need_lift   = 0.8 × (1 + 6 × dd) × ATR       # 낙폭 깊을수록 진입 문턱 ↑
k_trail     = 3.0 - 1.2 × vol_rank           # 변동성 높을수록 트레일 계수 ↓ (1.8~3.0)
k_probation = 1.2                            # 수습 기간 손절 (ATR 배수, 고정)
```

`k_trail` 을 변동성이 높을 때 **줄이는** 이유: ATR 자체가 이미 커져 있으므로
계수까지 키우면 손절폭이 % 기준으로 폭주한다. 클로드 1호가 NVDA 에서
MDD -41.90% 를 맞은 원인(§7-C)이 정확히 이것이다.

### 3-3. 미래 정보 유입 검증

- `rolling(252).rank(pct=True)`, `rolling(252).max()` 는 **모두 t 시점까지만** 참조한다
- `shift(-n)` 계열 연산 없음
- `ATR(14)` 는 Wilder EMA — 과거만 사용
- t 봉의 값으로 t 봉 종가에 체결 → **기존 5개 전략과 동일한 관례** (브리핑 §2 "당일 종가 체결")
- `min_periods` 를 252(vol_rank)·60(dd)로 명시해 워밍업 미성숙 구간을 차단 (§6-4)

**티커별 상수·분기문은 한 줄도 없다.** 위 3개 파라미터(0.8 / 6 / 3.0-1.2 / 1.2)는
전 종목 공통이며, 종목별 차이는 `dd` 와 `vol_rank` 값 자체에서 자동으로 나온다.

---

## 4. 진입·청산·사이징 로직

### 4-1. indicators()

```
atr        = ATR(14)
atr_pct    = atr / close
vol_rank   = atr_pct.rolling(252, min_periods=252).rank(pct=True)
dd         = 1 - close / close.rolling(252, min_periods=60).max()
low20      = close.rolling(20).min()
need_lift  = 0.8 * (1 + 6 * dd) * atr
k_trail    = 3.0 - 1.2 * vol_rank
```

### 4-2. entry(row)

```
if NaN(vol_rank, atr, dd): return False
return (close - low20) >= need_lift  AND  vol_rank < 0.5
```

조건 **2개**다. 제미나이 1호가 3개 AND 로 3년 17거래에 그친 전례(§7-E)를 피한다.

### 4-3. exit(row, state) — 단계적 축소 + 이중 손절

```
state.bars += 1                                    # exit() 는 보유 중 매 봉 1회 호출
gain_atr    = (close - entry_price) / atr          # 진입가 대비 ATR 단위 손익
dd_from_peak_atr = (peak - close) / atr            # 보유 중 고점 대비 하락 (ATR 단위)

# --- 수습 기간 (증명 전) ---
probation = (state.bars <= 10) AND (state.max_gain_atr < 1.0)
if probation AND (close <= entry_price - 1.2 * atr):
    return "수습 손절(1.2ATR)"                      # 전량

# --- 하드 손절 = 스윕 파라미터와 트레일링의 결합 ---
floor_price = entry_price * (1 + SL)               # SL = 스윕값 (-3% ~ -30%)
trail_price = peak - k_trail * atr
stop_price  = max(floor_price, trail_price)        # 더 높은(타이트한) 쪽이 발동
if close <= stop_price:
    return "손절/트레일링"                          # 전량

# --- 단계적 위험 축소 (래칫 다운) ---
if dd_from_peak_atr >= 1.0 AND tier == 0:
    tier = 1; return (0.33, "1단계 축소(고점-1ATR)")
if dd_from_peak_atr >= 1.8 AND tier == 1:
    tier = 2; return (0.50, "2단계 축소(고점-1.8ATR)")

return None
```

### 4-4. 손절이 실제로 발동하는가 — §5-1 의 죽은 방어선 문제

브리핑 §5-1: 3호·4호·클로드 1호는 ATR 트레일링이 항상 먼저 걸려 % 손절이
**한 번도 안 쓰인다**. 손절 스윕 15~30% 구간에서 수치가 완전히 고정된다.

**2호는 두 방어선을 `max()` 로 결합해 둘 다 살린다:**

```
stop_price = max(entry_price * (1 + SL),  peak - k_trail * atr)
```

| 국면 | floor (SL=-5%) | trail | binding |
|---|---|---|---|
| 진입 직후, 고변동 (ATR 큼) | 95 | 92.5 | **floor(SL)** ← 스윕이 작동 |
| 진입 직후, 저변동 | 95 | 97.5 | trail |
| 이익 구간 (고점 120) | 95 | 112.5 | trail |

즉 **포지션 초반·고변동 구간에서는 SL 이 binding** 하고, 이익이 쌓인 뒤에는
트레일링이 binding 한다. 손절 스윕 3%→30% 는 초반 손절폭을 직접 바꾸므로
**결과가 고정되지 않는다.** 이는 2단계 검증 항목이다 —
*"손절 8단계에서 수치가 고정되면 설계 실패로 간주하고 재작업"* 을 명시한다.

### 4-5. 사이징

- **부분 진입 불가** (§0) → 진입은 현금 100%
- 축소는 래칫: 100% → 67% → 33% → 0%
- 축소로 생긴 현금은 전량 청산 전까지 유휴 (엔진 제약)
- 이것이 MDD 억제에 기여한다: equity 에서 현금 부분은 변동하지 않는다

### 4-6. 예상 거래 수

`entry()` 신호 자체는 5.7Y 기준 종목당 43~73회로 많지만, **보유 중에는 호출되지
않으므로 실제 거래 수는 청산 이벤트 수가 결정한다.**
분할 축소 2단계가 있어 포지션당 최대 3행이 기록된다.
목표 구간(종목당 10~40)에 들어가는지는 2단계 실측 항목이다.
초과하면 `k_trail` 하한을 올려 청산 빈도를 낮춘다.

### 4-7. 인터페이스 적합성 — 확장 불필요

| 필요 기능 | 현 인터페이스로 가능한가 |
|---|---|
| 지표 계산 | ✅ `indicators()` |
| 진입 판정 | ✅ `entry(row)` → bool |
| 보유 봉 수 카운트 | ✅ `state["bars"] = state.get("bars",0)+1` (exit 이 매 봉 호출됨) |
| 고점 추적 | ✅ 엔진이 `state["peak"]` 관리 |
| 최대 이익 추적 | ✅ `state["max_gain_atr"]` 직접 저장 |
| 단계 기억 | ✅ `state["tier"]` 직접 저장 |
| 분할 청산 | ✅ `exit()` 가 `(비율, 사유)` 반환 |
| 손절 스윕 연동 | ✅ `self.SL` |
| 부분 진입 | ❌ **불가 — 설계에서 배제함** |

**엔진 수정 없이 구현 가능하다.**

---

## 5. 합격 기준 4개 자기 평가

| # | 기준 | 자기 평가 | 근거 |
|---|---|---|---|
| 1 | BTC Calmar ≥ 0.71 | **⚠️ 경계** | 아래 §5-1 |
| 2 | NVDA·AAPL·QQQ Calmar 격차 축소 | **△ 가능** | 아래 §5-2 |
| 3 | 4종목 회복 1000일 이내·미회복 0 | **○ 가장 자신 있음** | 아래 §5-3 |
| 4 | 4종목 MDD -30% 이내 | **⚠️ 경계** | 아래 §5-4 |

### 5-1. 기준 1 — BTC Calmar ≥ 0.71 (최난점)

Calmar = CAGR / |MDD|. MDD 를 기준 4 상한인 -30% 로 잡으면
**CAGR ≥ 0.71 × 30 = 21.3%** 가 필요하다.

| | CAGR | MDD | Calmar | 회복 |
|---|---|---|---|---|
| B&H | 19.25% | -76.63% | 0.25 | 846일 |
| 클로드 1호 | 22.28% | -31.46% | 0.71 | **921일 미회복** |
| **필요치** | **≥21.3%** | **≤30%** | **≥0.71** | **≤1000일** |

클로드 1호가 CAGR 22.28% 를 냈으므로 수익 측은 달성 가능 범위에 있다.
2호의 증분은 **2022 바닥 이후 41.4%p 먼저 탑승**(§2-1)이다. BTC 는 바닥에서
2024-03 고점 회복까지 약 +180% 상승했으므로, 이 구간 참여 시점이 41%p 앞당겨지면
CAGR 기여가 유의미하다.

**단, 그 대가로 오발 7회를 치른다.** 회당 -4~5%(수습 손절)면 누적 -30% 수준이고,
이것이 MDD 와 CAGR 을 동시에 갉아먹는다. **기준 1과 4는 서로 잡아당긴다** —
오발 손실을 줄이려 수습 손절을 좁히면 정상 진입도 잘려 CAGR 이 떨어지고,
넓히면 MDD 가 터진다. 여기가 이 설계의 가장 좁은 통로다.

### 5-2. 기준 2 — 나머지 3종목 Calmar 격차

| 종목 | 현 최고 | B&H | 좁혀야 할 격차 | 2호 전망 |
|---|---|---|---|---|
| QQQ | 0.36 | 0.47 | -0.11 | **가장 유망.** 재진입 123일→14일 단축, 낙폭 35%로 얕아 MDD 관리 쉬움 |
| AAPL | 0.28 | 0.56 | -0.28 | 중간. 재진입 57→8일, 낙폭 30% |
| NVDA | 0.54 | **0.97** | -0.43 | **가장 어려움.** B&H CAGR 64.35% 를 따라잡을 방법이 없다 |

NVDA 는 B&H 가 -66% 를 맞고도 541일 만에 회복하며 CAGR 64% 를 낸 자산이다.
**현금화 전략이 구조적으로 불리하다.** 2호의 목표는 역전이 아니라 **격차 축소**
(-0.43 → -0.30 수준)이며, 이는 기준 2의 요구사항과 일치한다.

### 5-3. 기준 3 — 회복 1000일 이내 (가장 자신 있음)

§1 에서 적었듯 **회복기간은 회복 구간 참여 여부가 직접 결정한다.**
2호는 바닥+8~22일에 복귀하므로 반등의 대부분을 먹는다.

BTC 구체 경로: 2022-11-21 바닥 → 2호 재진입 2022-12-13(+22일, 바닥대비 +12.6%)
→ BTC 가 2024-03-04 에 전고점 회복. MDD -30% 였다면 +43% 상승으로 회복되는데,
BTC 는 이 구간에 +180% 올랐다. **중간에 털리지만 않으면 회복은 여유 있다.**

리스크는 2023년 횡보 구간에서 트레일링에 걸려 나갔다가 더 높은 가격에
재진입하는 것이다. `k_trail` 을 변동성 기반으로 완화한 이유가 여기 있다.

### 5-4. 기준 4 — MDD -30% 이내

가장 위험한 종목은 BTC(-76% 구간 존재)다. 방어 수단:
1. 낙폭 비례 문턱으로 하락 중 진입을 7회로 제한
2. 수습 손절 1.2ATR 로 오발 1회당 -4~5% 제한
3. 단계적 축소(100→67→33%)로 급락 시 노출 자동 감소
4. SL floor 와 트레일링의 `max()` 결합

3번이 기존 전략에 없던 요소다. 클로드 1호는 -31.46%, 제미나이 4호는 -41.52% 였다.

---

## 6. 이 설계가 실패한다면 어디인가

### 가장 의심스러운 가정 (하나만 지목)

> **"조기 재진입이 회복기간을 단축하는 이득이, 가짜 반등에 물리는 손실보다 크다"**

이 가정은 **BTC 2022 구간에서 정면으로 반증될 수 있다.**

실측한 바로, 고정 문턱에서 BTC 는 하락 구간에 31회 신호를 냈고 **77% 가 20봉 후
음수**, 평균 -9.3%, 최악 -35.2% 였다. 낙폭 비례 문턱으로 7회까지 줄였지만
**7회가 모두 실패하면 누적 -30% 안팎**이고, 이는 기준 4를 단독으로 깨뜨린다.

즉 이 설계는 **"bear market rally 를 7번 중 몇 번은 잡는다"** 에 걸고 있다.
만약 7번 전부 가짜였다면 — 2022년 BTC 에서는 충분히 가능한 시나리오다 —
조기 재진입은 순수 손실이고, 기존 전략들이 "늦게 들어가는" 것이 사실은
**비용이 아니라 보험료**였다는 결론이 난다.

### 그 외 실패 가능 지점

1. **수습 손절이 정상 진입까지 자른다.** 1.2ATR 은 일봉 기준 좁다.
   진짜 바닥에서 들어가도 재시험(retest)에 걸려 나갈 수 있다. 그러면 조기 진입의
   이점을 하나도 못 먹고 오발 비용만 치른다. → 2단계에서 1.0/1.2/1.5 스윕 필요.

2. **`max()` 손절 결합이 의도와 달리 한쪽으로 고정될 수 있다.**
   `k_trail` 이 1.8~3.0 이고 ATR% 가 자산마다 다르므로, 특정 자산에서는
   floor 가 항상 binding(스윕은 살지만 트레일링이 죽음)하거나 그 반대가 될 수 있다.
   §5-1 문제를 반대 방향으로 재현하는 셈이다. → 2단계에서 binding 비율을 로깅해 확인.

3. **NVDA 는 애초에 이길 수 없는 자산일 수 있다.**
   CAGR 64.35%, 회복 541일. 어떤 현금화 전략도 구조적으로 불리하다.
   기준 2를 NVDA 에서 만족시키지 못하면 설계 자체가 아니라 **기준의 현실성**을
   재검토해야 할 수도 있다.

4. **단계적 축소가 회복을 방해한다.** 급락 시 67%→33% 로 줄였는데 곧바로 반등하면,
   줄인 만큼 반등을 못 먹는다. 유휴 현금은 전량 청산 전까지 재투입 불가(§0)이므로
   **축소는 되돌릴 수 없다.** 부분 진입이 막혀 있다는 제약이 여기서 비용으로 돌아온다.

5. **5.7Y 는 하락장 표본이 2022년 하나뿐이다.**
   이 설계는 사실상 "2022년 한 번의 약세장"에 맞춰 검증된다. §6-3(1년 표본의
   순위는 노이즈였다)과 같은 함정이 기간을 늘려도 여전히 존재한다.

---

## 7. (참고) 인터페이스 확장이 있다면 — 이번 라운드에서는 요청하지 않음

§0 의 제약이 풀리면 이 설계는 더 자연스러워진다. 다만 **엔진 수정 금지 지침에 따라
이번 구현에서는 사용하지 않는다.** 기록만 남긴다.

**최소 확장안 (backtest.py 3줄)**

```python
elif (w := strat.entry(row)):
    w = 1.0 if w is True else float(w)      # bool 하위호환
    shares = cash * w / (px * (1 + FEE))
    cash  -= cash * w
```

`entry()` 가 0~1 실수를 반환하면 목표 비중으로 진입. `True` 는 1.0 과 동일하므로
기존 5개 전략과 `BuyHold` 는 무수정 동작한다.

**왜 있으면 좋은가**: §2-3 의 "대가 지불"을 **빈도·손실폭이 아니라 크기**로 치를 수
있다. 낙폭이 깊을 때 `w = 1 - dd` 로 30%만 진입하면, 오발 7회의 손실이 1/3 로 줄고
동시에 조기 진입의 이점은 유지된다. 기준 1(Calmar)과 기준 4(MDD)가 서로 잡아당기는
§5-1 의 좁은 통로가 크게 넓어진다.

**확장 없는 현재 대안**: 낙폭 비례 진입 문턱(§2-3a) + 수습 손절(§2-3b).
크기를 못 줄이므로 **애초에 덜 들어가는 것**으로 대체한다. 성능 손실은 감수한다.

---

## 8. 2단계 실행 기록

1. ✅ `strategies.py` 에 `Claude2` 클래스 추가 (기존 전략 파일 무수정)
2. ✅ `CLASSES` 튜플 + `run_backtest.py` 의 `STRATEGIES` 에 등록 (2줄)
3. ✅ 파라미터 스윕 81조합 × 8셀 → §10
4. ✅ 검증 체크리스트 → §11
5. ✅ 미달 항목 그대로 보고 → §12

---

## 9. §6 의심 가정의 실제 결과 — **성립했다, 단 회복 구간을 포함해야만**

> 지목했던 가정: **"조기 재진입이 회복기간을 단축하는 이득이,
> 가짜 반등에 물리는 손실보다 크다"**

### 9-1. 하락 구간만 보면 가정은 거의 깨졌다 (2-A)

BTC-USD **2021-11-01 ~ 2023-01-31**, 9개 포지션 발생 (설계 예상 7회):

| 결과 | 값 |
|---|---|
| 포지션 수익/손실 | **4승 5패** |
| 누적 손익 | **+1.26%** (간신히 플러스) |
| 구간 MDD | **-29.95%** (기준 4 상한에 밀착) |
| 같은 구간 클로드 1호 | **+16.59% / MDD -9.54%** (2거래) |

**플러스 전체가 마지막 1건에서 나왔다.** 2023-01-10 진입 포지션이 +27.7%,
나머지 8개를 합치면 **-26.4%** 다. 브리핑 §5-3 이 경고한 "단일 셀 의존"이
거래 단위에서 재현됐다. 하락 구간만 보면 **"늦게 들어가는 것은 비용이 아니라
보험료"라는 반대 결론이 우세**했다.

### 9-2. 회복 구간을 포함하자 뒤집혔다 (5.7Y 최종)

| BTC-USD | CAGR | MDD | Calmar | 회복일 |
|---|---|---|---|---|
| 클로드 1호 | 22.28% | -31.46% | 0.71 | **921일 미회복** |
| **클로드 2호** | 21.00% | **-23.45%** | **0.90** | **572일 회복** |
| Buy & Hold | 19.25% | -76.63% | 0.25 | 846일 |

**가정 성립.** 하락장에서 잃은 것을 회복장 참여로 되찾는다는 전제가 확인됐다.
CAGR 은 클로드 1호보다 1.28%p 낮지만 MDD 를 8%p 줄여 Calmar 가 역전됐고,
설계의 주 목표였던 **미회복 상태가 해소**됐다.

다만 **4종목 중 3종목에서만** 성립했다. QQQ 는 회복일이 688→602 로 줄었는데도
CAGR 이 4.84%→4.19% 로 떨어져 Calmar 가 0.36→0.30 으로 후퇴했다.

### 9-3. 두 방어선 없이는 설계 자체가 성립하지 않는다 (2-A 실측)

BTC 2021-11~2023-01 구간, 방어 요소별 기여:

| 구성 | 누적손익 | 구간 MDD | 청산건 |
|---|---|---|---|
| **수습손절 O + 단계축소 O** | **+1.26%** | **-29.95%** | 21 |
| 수습손절 X + 단계축소 O | -2.11% | -32.27% | 21 |
| 수습손절 O + 단계축소 **X** | **-12.30%** | **-40.18%** | 9 |

단계축소가 **+13.6%p / MDD 10.2%p**, 수습손절이 **+3.4%p / MDD 2.3%p** 기여한다.
**단계축소를 빼면 기준 4(MDD -30%)가 단독으로 붕괴한다.** 이것이 2-B 에서
거래 빈도를 줄일 때 단계축소를 마지막 수단으로 미룬 근거다.

---

## 10. 파라미터 최종값과 선정 근거

```python
BASE_K = 0.8      KDD = 6.0      VR = 0.5
TRAIL_HI = 3.0    TRAIL_LO = 1.2
PROB_K = 1.0      PROB_BARS = 10
TIER_ATR = 1.0    TIER_FRAC = 0.50     # 원안 3단계(33/50/전량) -> 2단계로 변경
COOLDOWN = 10
```

### 10-1. 선정 절차 (사전 고정 기준)

81조합(`KDD`×`PROB_K`×`TIER_ATR`×`COOLDOWN`) × **8셀(4종목 × 2구간)**.
순위 기준은 **스윕 실행 전에 코드에 박아 고정**했다:

1. 8셀 중 합격 셀 수 (MDD > -30% AND 회복 ≤1000일 AND 미회복 아님)
2. Calmar **중앙값** (평균이 아니라 중앙값 — 한 셀 대박에 끌려가지 않게)
3. Calmar 최솟값

**8/8 합격은 4조합**이었고, 1·2순위에 따라 `KDD6 / PROB1.0 / TIER1.0 / CD10` 선정
(중앙 0.816, 최소 0.30). 이웃(±1스텝) 안정성도 확인 — 이웃 합격 평균 6.14/8, 최악 6/8.

### 10-2. 구조적으로 확인된 것 (단일 최고점이 아니라 경향)

| 파라미터 | 값별 합격셀 평균 | 판정 |
|---|---|---|
| `TIER_ATR` | 0.8 → 5.3 / **1.0 → 6.1** / 1.2 → 4.0 | 1.0 이 8/8 조합 **4개 전부**에 등장. 구조적 선택 |
| `KDD` | 3 → 4.1 / **6 → 5.8** / 10 → 5.4 | **KDD=3 은 8/8 조합 0개.** 낙폭 비례 문턱이 약하면 하락장에서 무너진다 |
| `PROB_K` | 1.0 → 5.3 / 1.2 → 5.1 / 1.5 → 5.0 | 완만. 1.0 선택 |
| `COOLDOWN` | 0 → 5.0 / 10 → 4.7 / 20 → 5.7 | 완만 |

### 10-3. 원안에서 바뀐 것 — 단계축소 3단계 → 2단계

원안은 `1.0ATR 에서 33% → 1.8ATR 에서 50% → 전량` 이었다.
**거래 빈도가 Calmar 미달의 원인**임을 확인하고(§11-1) 구조를 바꿨다.

| 구성 | Calmar | MDD | 청산건 | 포지션 |
|---|---|---|---|---|
| 3단계 33/50/전량 (원안) | 0.67 | -29.95% | 107 | 41 |
| (1) k_trail 상향 3.5-1.2vr | 0.51 | -30.53% | 95 | 36 |
| (2) 쿨다운 10봉 | 0.69 | -29.95% | 105 | 40 |
| **2단계 50%/전량 @1.0ATR + 쿨다운10** | **0.75** | **-27.47%** | **78** | 40 |

**방어를 약화시킨 것이 아니다.** 1.0ATR 시점에 33% 대신 50% 를 덜어내 더 일찍
크게 줄인 것이고, 그 결과 MDD 도 -29.95% → -27.47% 로 개선됐다.

---

## 11. 검증 체크리스트 결과

### 11-1. 거래 빈도가 Calmar 미달의 원인인가 → **그렇다**

수수료만 바꾼 동일 실행 (BTC 5.7Y):

| 수수료 | CAGR | MDD | Calmar |
|---|---|---|---|
| 0.1% | 20.00% | -29.95% | **0.67** |
| 0.05% | 20.87% | -29.53% | **0.71** |
| 0% | 21.74% | -29.10% | **0.75** |

수수료가 CAGR 1.74%p / **Calmar 0.079** 를 먹었다.

### 11-2. 손절 스윕에서 수치가 고정되는가 → **원래 고정됐고, 스윕 범위를 고쳐 일부 해소**

**원인은 전략이 아니라 스윕 범위였다.** ATR% 중앙값 실측 —
BTC 3.77%, NVDA 3.72%, **AAPL 2.18%, QQQ 1.58%**.
`PROB_K=1.0` 이면 손절폭이 1 ATR 인데, **저변동 자산에서는 이것이 스윕 하한
3% 보다 타이트**하다. 즉 어떤 SL 값도 binding 될 수 없었다.

수습 손절을 `max(SL floor, ATR)` 로 결합하는 수정도 시도했으나 **결과가 완전히
동일**했다 (ATR 쪽이 여전히 더 타이트하므로).

→ 스윕 범위를 `3~30%` → **`1/1.5/2/3/5/10/20/30%`** 로 변경 (브리핑 §8-4).

| 전략 (5.7Y, 고유값/32) | 범위 변경 후 |
|---|---|
| 제미나이 2호 (ATR 트레일링 없음) | **32/32** |
| 제미나이 1호 | 28/32 |
| 제미나이 4호 | 23/32 |
| 제미나이 3호 · 클로드 1호 | 21/32 |
| **클로드 2호** | **17/32** (QQQ 1→3, AAPL 1→4) |

**결론: 범위 탓이 주원인이지만 구조 탓도 남는다.** 퍼센트 손절만 쓰는 제미나이
2호는 전 구간 살아있고, ATR 트레일링을 쓰는 4개 전략은 3~5% 를 넘으면 여전히
포화된다. **ATR 폭보다 넓은 SL 은 그 구조에서 영원히 장식이다.**

`PROB_K` 를 키우면 스윕은 살아나지만 기준 1·4 가 함께 무너진다:

| PROB_K | 고유값(BTC/NVDA/AAPL/QQQ) | BTC Calmar | BTC MDD |
|---|---|---|---|
| **1.0 (채택)** | 2/2/1/1 (구범위) | **0.90** | **-23.45%** |
| 1.5 | 3/3/2/1 | 0.65 | -31.05% |
| 2.0 | 3/3/2/2 | 0.64 | -31.05% |

→ **PROB_K 는 1.0 유지.** 스윕 해상도보다 합격 기준을 우선했다.

### 11-3. 종목당 거래 수 → **포지션은 범위 내, 청산건은 초과**

| 종목 | 포지션 | 청산건 | 포지션당 |
|---|---|---|---|
| BTC-USD | **41** | 76 | 1.9 |
| NVDA | 35 | 63 | 1.8 |
| AAPL | 29 | 52 | 1.8 |
| QQQ | 33 | 55 | 1.7 |

목표 10~40(포지션 기준) 대비 **BTC 41개로 1개 초과**, 나머지는 범위 내.
청산건은 단계축소 때문에 포지션당 1.7~1.9건이다 (브리핑 §8-3 병기 규칙).

### 11-4. 기존 5개와의 월별 수익률 상관 (5.7Y)

| | BTC | NVDA | AAPL | QQQ | 평균 |
|---|---|---|---|---|---|
| 제미나이 1호 | -0.017 | 0.095 | 0.261 | 0.156 | **0.124** |
| 제미나이 2호 | 0.159 | -0.029 | 0.331 | 0.200 | **0.165** |
| 제미나이 4호 | 0.283 | 0.311 | 0.442 | 0.400 | 0.359 |
| 제미나이 3호 | 0.215 | 0.424 | 0.502 | 0.383 | 0.381 |
| **클로드 1호** | 0.621 | 0.571 | 0.346 | 0.535 | **0.518** |
| Buy & Hold | 0.651 | 0.697 | 0.549 | 0.599 | 0.624 |

역추세 계열(1·2호)과는 0.12~0.17 로 매우 낮다. **다만 클로드 1호와 0.518** 로,
"낮은 상관" 요구를 완전히 만족한다고 보기 어렵다. 둘 다 추세 국면에 오래
머무는 구조라 중복이 생긴다.

### 11-5. 미회복 조합 수 (5.7Y, 손절 5%)

| 전략 | 미회복 | 최장회복 |
|---|---|---|
| 제미나이 1호 | 3/4 | 1960일 |
| 제미나이 2호 | 2/4 | 2044일 |
| 클로드 1호 | 2/4 | 1066일 |
| 제미나이 3호 | 1/4 | 1110일 |
| 제미나이 4호 | 1/4 | 1823일 |
| **클로드 2호** | **0/4** | **602일** |

**6개 전략 중 유일하게 미회복 0건.** 설계의 주 목표를 달성한 지점이다.

### 11-6. 미래 정보 유입 재검증

- `rolling(252).rank(pct=True)`, `rolling(252).max()`, `rolling(20).min()` — 모두 t 까지만 참조
- 쿨다운 디바운스 루프 — 직전 신호 인덱스(`i - last`)만 비교, 미래 미참조
- `shift(-n)` 계열 없음
- `min_periods` 명시 (vol_rank 252 / dd 60)
- t 봉 값으로 t 봉 종가 체결 — 기존 5개 전략과 동일 관례

---

## 12. 미달·한계 (그대로 남김)

1. **기준 2 — QQQ 미달.** B&H 대비 Calmar 격차가 현 최고 -0.11 에서 **-0.17 로 악화**.
   회복일은 688→602 로 줄었지만 CAGR 이 4.84%→4.19% 로 떨어졌다.
   *QQQ 를 맞추기 위한 조정은 하지 않았다.*

2. **손절 스윕 해상도 부족.** 범위 확장 후에도 17/32 로 6개 전략 중 최저.
   `PROB_K` 를 키우면 해소되지만 기준 1·4 가 무너져 채택하지 않았다 (§11-2).

3. **거래 빈도 초과.** BTC 포지션 41개(상한 40), 청산건은 전 종목 50~76 건.
   수수료가 Calmar 0.079 를 먹고 있다 (§11-1).

4. **클로드 1호와 상관 0.518.** "기존 5개와 낮은 상관" 요구 불충족.
   역추세 계열과는 충분히 낮지만 추세 계열과는 중복된다.

5. **파라미터가 평가 구간 안에서 선정됐다 (in-sample).**
   81조합을 **5.7Y·3Y 8셀 전부를 보고** 골랐다. 순위 기준을 사전 고정하고
   중앙값·이웃 안정성으로 완화했지만 **선택 편의는 제거되지 않았다.**
   진짜 out-of-sample 검증은 2026-09-20 이후 데이터로만 가능하다.
   브리핑 §6-3("1년 표본의 순위는 노이즈였다")이 여기에도 적용된다.

6. **하락장 표본이 2022년 하나뿐이다.** 이 설계의 핵심 주장(조기 재진입)은
   사실상 단 한 번의 약세장에서만 검증됐다.
